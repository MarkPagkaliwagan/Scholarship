# Scholarship DB
# 1. Run tunnel first: bun scripts/connect.ts  (Mac/Linux)
#                   or: scripts\connect.ps1     (Windows)
# 2. Keep that window open, then use this DATABASE_URL

DATABASE_URL=postgresql://scholarship_admin:<password>@localhost:5433/scholarship_db
